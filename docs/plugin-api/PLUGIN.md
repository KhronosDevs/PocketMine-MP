# Plugin Development Guide

*This guide is a work in progress — the full plugin API walkthrough (plugin.yml, the Plugin base class, commands, events, permissions, scheduler, services via KernelAccessor, and ECS access) will live here. Until then, the API is usable and tested — see `tests/fixtures/demo-plugin/` for a minimal working plugin, and `docs/PLAN.md` / `docs/PROGRESS.md` for the API surface.*
