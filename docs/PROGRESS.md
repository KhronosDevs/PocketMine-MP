# Progress Tracker

**Plan Version:** docs/PLAN.md
**Last Updated:** 2026-08-13
**Current Phase:** 14.20 (multi-world) + storage formats — real Anvil/McRegion, infinite streaming
**Current Branch:** master
**Base Commit:** f636850 (master after the real-client fixes PR)

---

## Phase Status Overview

| Phase | Status | Branch | PR | Notes |
|-------|--------|--------|-----|-------|
| 0: Foundation | ✅ Done | foundation-bootstrap | #18 | Bootstrap, ports, ECS core, threading port |
| 1: ECS Components | ✅ Done | ecs-components-basic | #19 | 17 components, EntityRef, 4 systems |
| 2: Infrastructure Adapters | ✅ Done | adapter-implementation | #20 | Network, storage, worldgen, thread pool (NEW) |
| 3: Core Gameplay Services | ✅ Done | service-core-gameplay | #21 | Player, chunk, block, entity, combat, inventory |
| 4: **New Plugin API** | ✅ Done | api-ecs-plugin | #22 | **BREAKING** — EntityRef, Query, System, Event, Command, Scheduler |
| 5: Archetype Parallelism | ✅ Done | multithread-archetype-parallelism | #23 | Movement, Effect, Physics, AI parallel execution |
| 6: Region-Based Architecture | ✅ Done | region-based-architecture | #24 | RegionWorld, RegionThread, coordination, network threads |
| 7: Polish & Optimize | ✅ Done | polish-optimize | #25 | Storage, queries, memory, network batching, benchmarks |
| 8: **API ECS Rewrite** | ✅ Done | api-ecs-rewrite | #26 | **Complete ECS-based API rewrite** — Entity, World, Server, Inventory, Block, Scheduler, Command, Permission, Event, Plugin; PHPStan clean, legacy code fully removed |
| 8.5: World Rename | ✅ Done | api-ecs-rewrite | #26 | `Level` API renamed to `World` (Server getWorlds/getDefaultWorld/loadWorld, Block getWorld) |
| 9: Wire the Threading | ✅ Done | followups-9.2c-9.5 / 9.4b / 9.5b | #27 + #28 + next | RegionThread snapshot pipeline, NetworkThread batching, async chunk gen, migration + dynamic load balancing, pipeline hot-path optimization, scaling proof |
| 10: Complete Data & API Layer | ✅ Done | api-ecs-rewrite / 10.4-persistence-roundtrip / 10.3-heldslot | #26 + #30 + #31 | ChunkStore, BlockRegistry, ItemRegistry, WorldConfig; all `// Simplified` stubs removed; real block/chunk/inventory/world APIs; **world persistence round-trip proven**; held-slot single source of truth |
| 11-12: Tests & Gameplay Depth | ✅ Done | (multiple) | #32-#38 | Tests framework, gameplay services, combat/AI/crafting, plugins, networking end-to-end, permissions |
| 13: Performance + Real-Client Joinability | ✅ Done | followups-net-perms-perf + next | #38 + next | No busy-waiting (snooze), ECS hot paths, **RakNet connected layer ported** — the server now speaks real RakNet to a real 0.15.10 client |

---

## Phase 0: Foundation — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 0.0 | Create branch `foundation-bootstrap` with rollback anchor | ✅ | eb32941 | `git checkout -b foundation-bootstrap && git commit --allow-empty -m "chore: rollback anchor for foundation-bootstrap"` |
| 0.1 | `bootstrap.php` + manual DI composition root + `Kernel.php` | ✅ | — | No external DI lib; factory functions in bootstrap |
| 0.2 | Port interfaces in `src/pocketmine/port/` | ✅ | — | NetworkPort, StoragePort, WorldGenPort, ThreadingPort, CommandPort, EventPort, PluginPort + DTOs |
| 0.3 | ECS core in `src/pocketmine/domain/ecs/` | ✅ | — | Component, Resource, System, World, Query, Archetype, ComponentRegistry, ResourceRegistry, SystemScheduler, Entity, EntityBuilder |
| 0.4 | ThreadingPort + PmmpThreadPool adapter | ✅ | — | WorkerThread, ThreadedTask, FutureImpl, PmmpThreadPool implementing ThreadingPort |
| 0.5 | Establish tick-profiling baseline | ✅ | — | Added to Kernel::run(); measure_baseline.php script created |
| 0.6 | PHPStan config (level 5) for new code only | ✅ | — | `phpstan.neon` with paths: domain/, adapter/, port/ |
| 0.7 | Update PROGRESS.md, DECISIONS.md | ✅ | — | Record decisions from clarifying questions |

---

## Phase 1: ECS Components — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 1.0 | Create branch `ecs-components-basic` with rollback anchor | ✅ | 55d0fee | `git checkout -b ecs-components-basic && git commit --allow-empty -m "chore: rollback anchor for ecs-components-basic"` |
| 1.1 | Additional components: Rotation, Collision, Effect, Attribute, Inventory, AIState, Path | ✅ | 002c485 | Core gameplay components |
| 1.2 | EntityRef opaque handle for plugin API | ✅ | 002c485 | Stable reference across threads/migrations |
| 1.3 | Register all components in Kernel::registerBuiltinComponents() | ✅ | 002c485 | All 17 components registered |
| 1.4 | Component serialization helpers (for storage/network) | ✅ | 002c485 | ComponentSerializer with serialize/deserialize |
| 1.5 | New systems: EffectSystem, AISystem | ✅ | 002c485 | Parallel effect ticking, sequential AI |
| 1.6 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 2: Infrastructure Adapters — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 2.0 | Create branch `adapter-implementation` with rollback anchor | ✅ | 2798a89 | `git checkout -b adapter-implementation && git commit --allow-empty -m "chore: rollback anchor for adapter-implementation"` |
| 2.1 | `adapter-network-protocol84` — **NEW** Protocol84NetworkAdapter (direct RakLib UDP, protocol 84) | ✅ | 6036942 | Own network thread, packet encode/decode, PlayerRef mapping |
| 2.2 | `adapter-storage-anvil` — **NEW** AnvilStorageAdapter (direct .mca I/O) | ✅ | 6036942 | Region files, chunk serialization, level.dat |
| 2.3 | `adapter-worldgen-parallel` — **NEW** ParallelGeneratorAdapter | ✅ | 6036942 | Chunk gen, populate, light calculation |
| 2.4 | `adapter-thread-pool` — PmmpThreadPool (round-robin, parallelMap) | ✅ | 6036942 | Work distribution patterns |
| 2.5 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 3: Core Gameplay Services — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 3.0 | Create branch `service-core-gameplay` with rollback anchor | ✅ | d6ac09d | `git checkout -b service-core-gameplay && git commit --allow-empty -m "chore: rollback anchor for service-core-gameplay"` |
| 3.1 | `service-player-lifecycle` — PlayerJoinService, PlayerLeaveService, PlayerRespawnService | ✅ | 7ce67ce | EntityRef, PositionComponent, HealthComponent, InventoryComponent |
| 3.2 | `service-chunk-management` — ChunkLoadService, ChunkUnloadService, ChunkSendService | ✅ | 7ce67ce | StoragePort, ChunkData, NetworkPort |
| 3.3 | `service-block-interaction` — BlockBreakService, BlockPlaceService, BlockUpdateService | ✅ | 7ce67ce | ECS commands, CollisionComponent, InventoryComponent |
| 3.4 | `service-entity-management` — EntitySpawnService, EntityDespawnService, EntityInteractionService | ✅ | 7ce67ce | EntityRef, EntityBuilder, QueryBuilder |
| 3.5 | `service-combat` — CombatService, DamageService, KnockbackService | ✅ | 7ce67ce | AttributeComponent, HealthComponent, VelocityComponent |
| 3.6 | `service-inventory` — InventoryService, CraftingService, ContainerService | ✅ | 7ce67ce | InventoryComponent, ItemStack |
| 3.7 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 4: New Plugin API — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 4.0 | Create branch `api-ecs-plugin` with rollback anchor | ✅ | 0723b5c | `git checkout -b api-ecs-plugin && git commit --allow-empty -m "chore: rollback anchor for api-ecs-plugin"` |
| 4.1 | `api-ecs-plugin` — Plugin base class using EntityRef, QueryBuilder, System registration | ✅ | b625564 | Kernel access, service access, ECS integration |
| 4.2 | `api-event-system` — Typed event bus (not string-based), priority via PHP attributes | ✅ | b625564 | Event classes, listener registration, async dispatch |
| 4.3 | `api-command-system` — Command registration via attributes, typed arguments, tab completion | ✅ | b625564 | Command classes, argument parsing, permission checks |
| 4.4 | `api-scheduler` — Task scheduling via System registration or async Task submission | ✅ | b625564 | Repeating/delayed tasks, async task submission |
| 4.5 | `api-permissions` — Permission system integrated with EntityRef metadata | ✅ | b625564 | Permission checks on EntityRef, default permissions |
| 4.6 | `api-world-access` — Chunk/Block/Entity access via Query, not Level/Entity getters | ✅ | b625564 | QueryBuilder, chunk iteration, block/entity access |
| 4.7 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 5: Archetype Parallelism — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 5.0 | Create branch `multithread-archetype-parallelism` with rollback anchor | ✅ | ca5ca63 | `git checkout -b multithread-archetype-parallelism && git commit --allow-empty -m "chore: rollback anchor for multithread-archetype-parallelism"` |
| 5.1 | `multithread-movement` — PositionComponent + VelocityComponent parallel updates | ✅ | 3f35600 | Double-buffered PositionComponent for parallel writes |
| 5.2 | `multithread-effects` — EffectComponent parallel tick | ✅ | 3f35600 | Already PARALLEL, optimize archetype iteration |
| 5.3 | `multithread-physics` — PhysicsSystem parallel broad-phase collision | ✅ | 3f35600 | SpatialIndex per archetype, double-buffered VelocityComponent |
| 5.4 | `multithread-ai` — AISystem parallel per archetype | ✅ | 3f35600 | Pathfinding via ThreadingPort, double-buffered AIState |
| 5.5 | `multithread-chunk` — ChunkParallelSystem for block updates | ✅ | 3f35600 | Per-chunk parallel block updates |
| 5.6 | `multithread-scheduler` — SystemScheduler parallel execution via ThreadingPort | ✅ | 3f35600 | AwaitAll on parallel systems |
| 5.7 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 6: Region-Based Architecture — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 6.0 | Create branch `region-based-architecture` with rollback anchor | ✅ | 37860e3 | `git checkout -b region-based-architecture && git commit --allow-empty -m "chore: rollback anchor for region-based-architecture"` |
| 6.1 | `region-world` — RegionWorld ECS World slice per spatial region (16×16 chunks) | ✅ | b33d6a1 | RegionWorld with spatial bounds, chunk ownership |
| 6.2 | `region-thread` — RegionThread per region with independent ECS tick loop | ✅ | b33d6a1 | Dedicated thread, command/sync queues |
| 6.3 | `region-coordination` — CoordinationThread for entity migration, global events | ✅ | b33d6a1 | Cross-region migration, plugin dispatch, chunk coordination |
| 6.4 | `region-network` — NetworkThread for RakLib I/O + packet encoding | ✅ | b33d6a1 | Dedicated network thread, batch encoding |
| 6.5 | `region-migration` — Cross-region entity migration with snapshots | ✅ | b33d6a1 | EntityRef transfer, component snapshot |
| 6.6 | `region-load-balancing` — Dynamic region splitting/merging | ✅ | — | Workload-aware partitioning |
| 6.7 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 7: Polish & Optimize — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 7.0 | Create branch `polish-optimize` with rollback anchor | ✅ | 6c291d9 | `git checkout -b polish-optimize && git commit --allow-empty -m "chore: rollback anchor for polish-optimize"` |
| 7.1 | `optimize-archetype-storage` — Compact component arrays, reduce indirection | ✅ | 04f0ea3 | Struct-of-arrays, eliminate object overhead |
| 7.2 | `optimize-query-performance` — Query caching, archetype indexing | ✅ | 04f0ea3 | Query plan caching, archetype bitmap index |
| 7.3 | `optimize-memory-layout` — Struct-of-arrays, reduce object allocation | ✅ | 04f0ea3 | Flat arrays, object pooling, weak refs |
| 7.4 | `optimize-network-batching` — Batch NetworkSyncComponent flushes across regions | ✅ | 04f0ea3 | Cross-region packet batching |
| 7.5 | `benchmark-profile` — Full profiling, tick rate analysis, scalability testing | ✅ | 04f0ea3 | Load testing, flame graphs, regression tests |
| 7.6 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Phase 8: API ECS Rewrite — Detailed Steps

| Step | Task | Status | Commit | Notes |
|------|------|--------|--------|-------|
| 8.0 | Create branch `api-ecs-rewrite` with rollback anchor | ✅ | 29b5081 | `git checkout -b api-ecs-rewrite && git commit --allow-empty -m "chore: rollback anchor for api-ecs-rewrite"` |
| 8.1 | Entity API: Entity, Player, Living, Monster, Animal, Zombie, Skeleton, Creeper, Pig, ItemEntity, EntityFactory | ✅ | 2079019 | EntityRef-based with component accessors |
| 8.2 | Level API: Level with chunk/entity management via ECS services | ✅ | 2079019 | ChunkLoadService, EntitySpawnService, etc. |
| 8.3 | Server API: Server singleton with ECS integration, service access | ✅ | 2079019 | 18 services accessible via KernelAccessor |
| 8.4 | Inventory API: Inventory, ItemStack with full inventory management | ✅ | 2079019 | Stacking, NBT, enchantments |
| 8.5 | Block API: Block with level integration | ✅ | 2079019 | Block operations via Level |
| 8.6 | Scheduler API: Scheduler with task management and system registration | ✅ | 2079019 | Task management + System registration |
| 8.7 | Command API: Command, CommandMap, CommandSender, attributes | ✅ | 2079019 | Attribute-based commands |
| 8.8 | Permission API: Permission, PermissionManager with inheritance | ✅ | 2079019 | EntityRef metadata-based checks |
| 8.9 | Event API: EventBus, typed events (Player, Block, Entity), attributes | ✅ | 2079019 | Typed events with EventHandler attribute |
| 8.10 | Plugin API: Plugin base, PluginManager, PluginDescription, Logger, Config | ✅ | 2079019 | KernelAccessor provides full ECS/services/ports |
| 8.11 | World API: WorldAccessor with ECS query integration | ✅ | 2079019 | QueryBuilder integration |
| 8.12 | Update PROGRESS.md | ✅ | — | Track progress |

---

## Decision Log (links to docs/DECISIONS.md)

| ID | Decision | Date | Context |
|----|----------|------|---------|
| D001 | Manual factory-based composition root | 2026-08-06 | No external DI library |
| D002 | PHPStan level 5+ for new/refactored code only | 2026-08-06 | No baseline; fresh start |
| D003 | No test suite initially | 2026-08-06 | Focus on architecture first |
| D004 | pmmpthread v6.3.0 | 2026-08-06 | Confirmed version |
| D005 | Protocol 84 = frozen boundary, no tests | 2026-08-06 | Must remain untouched |
| D006 | Establish tick-profiling baseline early | 2026-08-06 | Before any threading work |
| D007 | **New Plugin API = breaking changes, no legacy compat** | 2026-08-07 | Clean ECS-based API (EntityRef, Query, System) |
| D008 | **Double-buffered components for parallel writes** | 2026-08-07 | PositionComponent, VelocityComponent, AIStateComponent |
| D009 | **Region size: 16×16 chunks (256×256 blocks)** | 2026-08-07 | Balance between parallelism and migration overhead |
| D010 | **Struct-of-arrays layout for archetype storage** | 2026-08-07 | Flat arrays for cache performance |

---

## Branch/Commit/PR Tracking

| Branch | Base Commit | Current Commit | PR # | Status |
|--------|-------------|----------------|------|--------|
| foundation-bootstrap | eb32941 | ac5b861 | #18 | ✅ Merged |
| ecs-components-basic | ac5b861 | 378400c | #19 | ✅ Merged |
| adapter-implementation | 3b66cf1 | 63a1b02 | #20 | ✅ Merged |
| service-core-gameplay | 49cf545 | 7ce67ce | #21 | ✅ Merged |
| api-ecs-plugin | 8f3a171 | fc1c65d | #22 | ✅ Merged |
| multithread-archetype-parallelism | 43996ce | 33f6ff6 | #23 | ✅ Merged |
| region-based-architecture | c7e445f | 1c1baf4 | #24 | ✅ Merged |
| polish-optimize | 942082e | 582c377 | #25 | ✅ Merged |
| api-ecs-rewrite | 1cfc650 | 2079019 | #26 | 🔄 Active (PR opened) |

---

## Next Actions

1. ✅ Phase 0, 1, 2, 3, 4, 5, 6, 7 complete (PRs #18, #19, #20, #21, #22, #23, #24, #25 merged)
2. ✅ Create `api-ecs-rewrite` branch with rollback anchor
3. ✅ Implement Entity API (Entity, Player, Living, Monster, Animal, Zombie, Skeleton, Creeper, Pig, ItemEntity, EntityFactory)
4. ✅ Implement Level API (Level with ECS services)
6. ✅ Implement Server API (Server singleton with ECS integration)
7. ✅ Implement Inventory API (Inventory, ItemStack)
8. ✅ Implement Block API (Block with level integration)
9. ✅ Implement Scheduler API (Scheduler with task management)
10. ✅ Implement Command API (Command, CommandMap, attributes)
11. ✅ Implement Permission API (Permission, PermissionManager)
13. ✅ Implement Event API (EventBus, typed events, attributes)
14. ✅ Implement Plugin API (Plugin, PluginManager, Logger, Config)
14. ✅ Implement World API (WorldAccessor)
15. ✅ Remove all legacy PocketMine code (1,100+ files: Player, Server, Entity, blocks, items, plugins, dead packets)
16. ✅ Fix threading layer for pmmpthread v6.3 (ThreadSafe-based FutureImpl, synchronous pool submit)
17. ✅ Fix runtime landmines (Info constants, Binary ENDIANNESS/bcmath, spawn() EntityRef, service layer)
18. ✅ PHPStan 0 errors (was 142); lint clean; boot/full/API tests pass; benchmark mean 0.077 ms
19. ✅ Update docs, commit, push, open PR #26
20. ✅ Rename API `Level` → `World` (Server world methods, Block getWorld, Events import cleanup)
21. ✅ **Phase 10: real data layer** — new `core/resource/` resources: ChunkStore (binary-string block store), BlockRegistry (~70 block property table), ItemRegistry (stack/durability/names), WorldConfig (time/seed/spawn/rules); ChunkLoad/Unload, BlockBreak/Place services operate on real chunk data; api World/Block/Inventory wired end-to-end
22. ✅ Remove **all** `// Simplified` and `// Would` placeholders (Server uptime/TPS/dispatch, Player sendMessage/kick via NetworkPort, PluginCommand executors, Animal owner/breed, Plugin config files, Scheduler ownership, AI attack, ItemStack registry lookups, `canAddItem` recursion bug)
23. ✅ **Tests framework** — `tests/` with zero dependencies (`tests/run.php` + `helpers.php`, per-process isolation): 6 files, 21 tests, 277 assertions; `composer test` wired. Covers ChunkStore, registries, gameplay services, ECS core, and the pipeline
24. ✅ **Phase 9.1: lockstep region pipeline** — kernel mirrors Position+Velocity entities to the `RegionThread` each tick; worker integrates movement+gravity exactly once per `tick` command (seq-tagged); results merged back with a bounded sync point, stale results dropped. Determinism gate: **0 mismatches** vs main-thread simulation; apply mode (worker authoritative) verified exact. Flags: `setRegionPipelineEnabled` / `setRegionPipelineApplyMode`; stats via `getRegionPipelineStats()`
25. ✅ **Fixed 3 real ECS bugs found by the new tests** — `EntityBuilder::at()` missing import; `Archetype::allocateIndex()` off-by-one (first entity at index -1, never processed by systems); missing archetype migration when an entity's component set changes after spawn (teleport broke system queries)
26. ✅ **Binary snapshot transport (9.2a)** — JSON replaced with a compact binary protocol (52 bytes/entity, one batched message per tick per region; floats round-trip bit-exactly). At 1000 entities the gate now receives **100,000/100,000 results in-window, 0 lagged, 0 mismatches** and drops from 130.9 ms → **10.1 ms** per tick; apply mode 10.5 ms, all results applied
27. ✅ **Archetype reconcile dirty-flag (9.2b)** — `Entity::set/remove` flip a per-entity flag so `World::reconcileArchetypes` skips untouched entities (O(entities) bool reads per tick instead of array_keys+sort)
28. ✅ **ItemStack unification (10.2)** — api `Inventory`, `Block`, `World::dropItem`, `ItemEntity` all speak `api\inventory\ItemStack`; `toCore()`/`fromCore()` convert at the boundary; core component type confined to the storage layer
29. ✅ **Diff-only mirror (9.2c)** — kernel tracks worker-stored snapshots and predicts post-integration state with bit-exact `integrateOnce`; unchanged entities skip re-mirror (no double integration in apply mode — drain records applied state). At 1000 entities × 100 ticks: **mirrored 1,000 (first tick only) / skipped 99,000, 0 mismatches, 0 lagged**; gate tick 10.1 ms → 4.9 ms
30. ✅ **NetworkThread batching (9.3)** — adapter uses the **kernel's single NetworkThread** (injected via `setNetworkThread`; adapter no longer spawns its own); outbound frames **coalesced per destination** into batched datagrams (100 frames → 2 sendto calls, all bytes preserved)
31. ✅ **Async chunk generation (9.4)** — `ParallelGeneratorAdapter` refactored to a **pure static** generator (`generateChunkPure`); each chunk is a `ChunkGenerationTask` (`extends pmmp\thread\Runnable`) on a **real pmmpthread `Pool`** (lazy, nproc−2 workers); results return via a serialized string in a `ThreadSafe` cell; deterministic across worker threads
32. ✅ **Scale-correctness test (`tests/07`)** — 1000 entities apply-mode over 10 ticks: all 10,000 results applied, exact integration positions, **0 mismatches / 0 lagged**; gate at 1000 entities stays exact with diff-only; async-gen determinism verified; suite now **7 files / 25 tests / 371 assertions**
33. ✅ **Batch gen wired (9.4)** — `ChunkLoadService::loadChunks()` bulk-loads through the parallel `WorldGenPort::generateChunks()` path (shared `materializeChunk`); `loadChunk()` delegates. Benchmark `measure_chunkgen.php`: **256 chunks par 7.5–8.1× vs sequential, ~3× vs main-thread pure; 2048 chunks 8.4×** (558 ms vs 4.7 s), identical terrain across pure/seq/par
34. ✅ **Noise perf fix** — terrain height noise multiplied un-masked ints past 2^63 → PHP float conversions (`&`/`>>` casts) were 46× slower single-threaded and ~1,000× slower under 8-worker concurrency (pool appeared serialized). Rewritten with 31-bit-masked int hashing (`hash31`); generator is int-only and the pool now genuinely parallelizes. Also: allocation-lean flat/terrain block builders (bit-identical output verified), `workerCount` capped at 4, results decoded inside the `Pool::collect()` callback (safe pmmp pattern; cells released per-task so big batches stay within 128M)
35. ✅ **Cross-region migration wired (9.4b)** — kernel creates `regionCount` column-split regions (default 1); diff-only mirror tracks owning region (`pipelineRegion`); boundary-crossers are despawned from the old region and pushed to the new region's `migrationQueue` before the tick (drained at tick-processing time, ordering guaranteed). `tests/08_migration_test.php`: entities crossing boundaries stay exact, 0 mismatches/lagged
36. ✅ **Dynamic region load balancing (9.4b)** — region bounds are `ThreadSafe` (mutable after start); a region over `maxEntitiesPerRegion` splits at the entity-weighted median chunk (both halves must clear the merge floor, preventing split/merge oscillation — 5000-entity workload: 23 splits/17 merges → 6 splits/0 merges); a region under threshold/5 folds into its adjacent neighbor (bounds absorb, bookkeeping resets, worker joined). Splits spawn a fresh `RegionThread` on the fly; migration rides the existing despawn+migrate path. `run()` resumable via `setAutoShutdownOnRun(false)`. `tests/09_region_balance_test.php`: 1500 entities split to 4+ balanced regions, 0 mismatches/lagged; apply mode exact; sparse population merges back down
37. ✅ **Pipeline hot-path optimization (9.5b)** — flat stored-state arrays (mirror loop allocates nothing), per-entity (chunk, region) cache + epoch (skips `ownsChunk` ThreadSafe reads in steady state), gated balance bookkeeping (counts only when a split threshold is set; chunk-Xs only over threshold), block results wire layout (kernel: 2 `unpack` per batch; worker integrates on packed doubles). `measure_pipeline` gains per-phase profiling. Scale: **5000 entities apply → 15.7–17.4 ms (from 26.9), 600,000/600,000 applied, 0 mismatches, 0 lagged**; gate 1000 entities stays exact with diff-only mirroring (1,000 mirrored / 99,000 skipped)
38. ✅ **World persistence round-trip (10.4)** — `tests/10_persistence_roundtrip_test.php` proves the storage layer end-to-end: (1) full DTO equality through a **fresh adapter instance** (server-restart scenario) with sections, meta, light, biomes, heightmap, entities (string ids + components) and tile entities; (2) **negative chunk coordinates** (region-file naming from arithmetic-shifted coords); (3) **re-save idempotency**; (4) **service-level** load → mutate → save → reload returns the identical DTO + block/biome state; (5) **cross-kernel**: kernel A saves a mutation, a fresh kernel B boots and reads it back. **Fixed 3 real storage bugs the tests caught**: region timestamp table was written at byte 8192 — over the first chunk's data sector, so every save corrupted that chunk's length field (timestamps now write to the correct table at `HEADER_SIZE/2`); `BinaryStream` lacked `getDouble`/`putDouble` (entity positions crashed on save); entity/tile ids were cast to `int` and written as varints via `getVarInt`/`putVarInt` — methods that don't exist in this codebase (ids now round-trip as strings, UUID-style ids survive). Padding arithmetic also fixed (file exactly sector-aligned). Suite now **10 files / 35 tests / 596 assertions**
39. ✅ **Held-slot single source of truth (10.3)** — `InventoryComponent::$heldSlot` (declared but never used) is now the single source for the held slot: the API facade, `InventoryService`, `BlockBreakService`, and `EntityInteractionService` all read/write the component property; the metadata `heldSlot` key is gone. Bound policy centralized in a new `InventoryComponent::setHeldSlot(int): bool` (validated against inventory size) — the facade was previously unbounded while the service enforced a hotbar bound. Bonus: held slot now persists with the component (`ComponentSerializer`). `tests/03` asserts the facade writes the component and metadata carries no `heldSlot`. **Phase 10 complete.** Suite now **10 files / 35 tests / 599 assertions**
40. ✅ **Memory profiling & budget (11.3)** — the loaded-chunk cap was a dead constant + a stub; now **enforced**: `ChunkLoadService` enforces `maxLoadedChunks` (configurable) after every bulk load (the single choke point), and `ChunkUnloadService::unloadUnusedChunks` evicts the **oldest** resident chunks FIFO — each persisted to disk before leaving memory, so eviction is lossless (`tests/11`: a marker block set in a loaded chunk survives eviction and reload from disk). `ChunkStore` gains `getOldestLoadedChunk()` + `getMemoryEstimate()` (~160 KB/chunk payload). **Archetype free-list leak fixed**: freed indices were tracked per-component-type but `allocateIndex` only ever popped the FIRST type's list, so every other list accumulated duplicate stale entries forever — now one shared flat list (index reuse verified: high-water stays flat across despawn/respawn churn). New `getMemoryProfile()` on the kernel (entities, archetype stats with capacity/valid/wasted slots, chunk count + bytes, PHP peak) + `measure_memory.php` profiler: per-entity ~1–2 KB, 200 loaded chunks → exactly 64 resident at the 64-chunk budget (12 MB payload), index high-water flat at 8000 after 8000→0→2000 churn. Suite now **11 files / 39 tests / 619 assertions**
41. ✅ **Combat / death / drops (12.2)** — the three duplicated death paths (`CombatService`, `DamageService`, `EntityInteractionService` each had their own `handleDeath`, an **empty** `dropLoot()` stub, and a `dropExperience()` that read a nonexistent `$entity->x`) now funnel through `CombatService`: `applyDamage` fires a **cancellable `EntityDamageEvent`** (plugins can cancel or override damage) before armor reduction + knockback, and death fires **cancellable `EntityDeathEvent` / `PlayerDeathEvent`** (with killer + death message) before loot/XP/despawn. `dropLoot` is real: **inventory contents are dropped as item entities** (scattered, then cleared) + a **mob loot table** (zombie/skeleton/creeper/spider/cow/pig/sheep/chicken → flesh, bones, arrows, gunpowder, leather, wool…); XP orbs spawn at the correct position (was `$entity->x`). `CombatService::kill()` and `Entity::kill()` route through the same pipeline; `DamageService` + `EntityInteractionService::attack` delegate to it. **Bonus bug found by the new test**: `World::applyPendingComponents()` crashed on boolean tag components (`method_exists(true, …)`) — now guarded with `is_object`. `tests/12_combat_test.php`: 7 tests / 41 assertions (cancelled damage leaves health untouched, event damage override, death drops + XP + despawn, cancellable death keeps the entity alive with no drops, player death message + inventory drop, `kill()` skips the damage event + zeroes health for `isAlive()`, double-death re-entrancy guard, `EntityInteractionService::attack` weapon-damage delegation, `Entity::kill()` facade route, `DamageService` full-pipeline delegation). Suite now **12 files / 46 tests / 660 assertions**
42. ✅ **Real AI (12.1)** — `AISystem` is now a **sequential main-thread system** (was a parallel worker that could not touch the combat pipeline) and mobs are actually wired to it: `EntitySpawnService` attaches an `AIStateComponent` to every hostile (Zombie/Skeleton/Creeper/Spider) and passive (Cow/Pig/Sheep/Chicken) mob at spawn with **per-mob stats** from a table (health, attackDamage, speedModifier, followRange, attackRange, retreatHealthPercent); hostile mobs also get `MonsterTag`. The system now maintains the (previously dead) **SpatialIndex** each tick, **acquires the nearest living player** within follow range as a target, chases at full speed, and attacks **through the 12.2 combat pipeline** — `CombatService::applyDamage` with `CAUSE_ENTITY_ATTACK` (damage event, armor, knockback, death/loot) — gated by a **20-tick attack cooldown** (`AIStateComponent::attackCooldown/attackCooldownMax`). Mobs **flee the nearest player when hurt** below `retreatHealthPercent` (passive mobs panic at 30% health; hostile zombies do not retreat). Targets are validated every tick (dead/despawned/out-of-range clears). **Bonus bug found by the new tests**: a cached `Query` holds a **frozen entity array** — any spawn/despawn left every cached query (AI included) iterating a stale snapshot forever; `World::flushEntityChanges` now clears the query cache whenever the entity set changes (steady-state ticks keep the cache). `tests/13_ai_test.php`: 9 tests / 28 assertions (AI state + stats at spawn, target acquisition within range / none beyond it, chase closes distance, attack cooldown caps hits in 100 ticks + gates even the first hit, fatal AI attack kills through the pipeline with death event, hurt cow flees, target death clears the mob target + idle velocity zeroed, healthy passive mob never acquires/chases). Suite now **13 files / 55 tests / 688 assertions**
43. ✅ **Crafting / recipes (12.3)** — recipes now live in **registry data** instead of a hard-coded 1-item inline table: new `core/resource/RecipeRegistry` (shaped recipes: pattern + char→ingredient key, wildcard meta support, **any-offset matching — a recipe matches at any position inside a larger grid like vanilla, not just the top-left**, and **extra grid items outside the pattern reject the match**) with `registerBuiltinRecipes()` in the Kernel (planks, sticks, crafting table, furnace, chest, wooden pickaxe/axe/sword, torches — plugins can register more via `registerShaped`). `CraftingService::craft()` rewritten: 3x3 grids require the crafting table flag, ingredients are verified against the player inventory, and the flow is **atomic** — a non-mutating `InventoryComponent::canAddItem()` (new, shared with the api facade pattern) checks the result fits BEFORE anything is consumed, then ingredients are consumed and the result is added (the old code returned a result it never placed, plus had a broken `getComponentRegistry()->get(InventoryComponent::class)` line). **Bugs found by review + tests**: `ContainerService::transferItem` moved whole stacks but removed only ONE item from the source (default `remove($slot)` count=1) — source counts now capture the stack size before the transfer; `craft()` handed `add()` the **registry's own stored result ItemStack**, so repeated crafts mutated the registered recipe and returned a residual-count stack — the result is now cloned before `add()` (which consumes the passed stack's count) and a pristine copy is returned; **`InventoryComponent::add()` created over-stack slots** (100 items in one slot) — it now splits over-stack remainders across empty slots so `add()` always fits exactly what `canAddItem()` promises. `tests/14_crafting_test.php`: 9 tests / 44 assertions (registry matching incl. wildcard meta + wrong-shape rejection + **any-offset placement**, craft consumes + result lands in inventory, null on missing ingredients/mismatch, 3x3 requires table, no-space atomicity, extra-items grid rejected, **over-stack result splits across slots with no over-stack slot**, repeated crafts don't corrupt the registry / return full counts, container transfer both directions). Suite now **14 files / 64 tests / 734 assertions**
44. ✅ **Plugins + unified commands (12.4)** — plugins are real: `PluginManager` (the kernel's plugin port — `Kernel::getPluginManager()` and `Server::getPluginManager()` both return the single stable instance) loads a plugin from a **directory or a `.phar` archive** (`.jar` is explicitly rejected — Java binaries are not PHP plugins) by parsing **`plugin.yml`** (`name`, `version`, `author`, `description`, `main`, `api` min-version, `depend`/`softdepend`, `commands`), registering a PSR-0 autoloader over the archive's `src/` directory, instantiating the `main` class (must extend the `Plugin` base class) and running `onLoad()` → `onEnable()`. Guards: **api mismatch** (plugin api > `Kernel::API_VERSION` 2.0.0 rejected), **missing dependencies** (`depend` must be loaded+enabled), duplicate names, missing/invalid `plugin.yml`, missing or non-`Plugin` main class. `plugin.yml` `commands:` entries auto-register `PluginCommand`s bound to the plugin's `onCommand()` hook so metadata (description/usage/aliases/permission) stays declarative; class-based commands registered via `registerCommand()` in `onEnable()` land in the **same map**. **One unified server-wide command map**: the api `CommandMap` now implements `CommandPort` (returned by `createCommandPort()`), the api `EventBus` implements `EventPort`, the api `PluginManager` implements `PluginPort` — the duplicate `ConsoleCommandAdapter` / `PluginManagerAdapter` / `PluginEventAdapter` / port-layer `Command` / `CommandSender` / `Plugin` / `PluginDescription` classes are **deleted** (no more two disconnected command maps). `Server::dispatchCommand($line, $sender)` resolves the sender (a `CommandSender`, a **player name** → `PlayerCommandSender`, or console), and **player dispatches fire the cancellable `PlayerCommandPreprocessEvent`** before execution. `disablePlugin()` unregisters the plugin's yml + class commands and frees its name for reload. Fixtures: `tests/fixtures/demo-plugin/` (directory) + built `tests/fixtures/demo-plugin.phar` (archive). `tests/15_plugin_test.php`: 14 tests / 60 assertions (dir + phar load, metadata + lifecycle, yml + class commands server-wide, console/player/alias dispatch, preprocess fire + cancel, deps, api, `.jar` rejection, duplicate/missing/invalid cases, disable + reload). Suite now **15 files / 78 tests / 794 assertions**
45. ✅ **Full block registry coverage (10.1)** — the registry went from 112 entries to **191 explicit entries covering every real protocol-84 block id** (all 189 real ids: 192 canonical from the legacy `BlockIds` minus 12 reserved/unused ids + 9 ids that table omits but 0.15 has: 36 piston arm, 119 end portal, 122 dragon egg, 130 ender chest, 138 beacon, 160 stained glass pane, 166 barrier, 168 prismarine, 169 sea lantern), each with 0.15 data (hardness, resistance, light, opacity, flammability, tool + tier, drops, XP). Newly covered: trapdoor, iron bars, glass panes, huge mushrooms, stems, nether wart, brewing stand, cauldron, end portal/dragon egg, redstone lamps (lit 15 / unlit 0), wooden slabs, cocoa, sandstone stairs, ender chest, tripwire, wood-variant stairs, beacon, cobblestone wall, flower pot, crops, buttons, skull, anvil, trapped chest, weighted plates, comparators, daylight sensors, redstone block, quartz ore (→ quartz item, XP 2), hopper, quartz block/stairs, activator rail, dropper, stained clay/glass pane, leaves 2/log 2, acacia/dark oak stairs, slime, barrier, iron trapdoor, prismarine, sea lantern, hay, carpet, hardened clay, coal block, packed ice, double plants, red sandstone + stairs + slabs, all wood fence gates and doors. **Block-state metadata layer**: `BlockRegistry::STATES` describes how meta encodes state — **slab/double_slab** (meta & 7 = material variant — stone/sandstone/wooden/cobblestone/brick/stone brick/quartz/nether brick, oak→dark oak woods, red sandstone; bit 3 = top half), **stairs** (bits 0-1 facing, bit 3 upside-down), **door** (bits 0-1 facing, bit 2 open, bit 3 top half) — with accessors (`getStateKind/StateName/StateVariants/SlabMaterial/StairFacing/isSlabTop/isStairUpsideDown/isDoorOpen/isDoorTopHalf`) on the registry AND the api `Block` facade (`getStateName()` returns e.g. "Sandstone Slab" at meta 1). **Placement wired**: `BlockPlaceService` resolves a slab's top/bottom bit from the clicked face (`applyPlacementMeta`) AFTER consuming the plain item meta from inventory, so placing a stone slab against the bottom face stores meta 8 and charges the material-meta 0 item. `tests/16_block_registry_test.php`: 7 tests / 457 assertions (full 189-id coverage + reserved fallback, new-block property + drop spot checks, slab/stairs/door state semantics, face→meta resolution, service-level placement writes state meta + consumes inventory, api facade state access). Suite now **16 files / 85 tests / 1251 assertions**
46. ✅ **Networking end-to-end (12.5)** — a real client can connect and play through the full protocol-84 flow. New `core/service/NetworkSessionService` (the kernel's session loop) drives the handshake from the adapter's `ThreadSafe` inbox: **login** (LoginPacket → protocol/version checks → `PLAY_STATUS_LOGIN_SUCCESS` + `StartGame`/`SetTime`/`SetSpawnPosition`/`SetDifficulty`/`PlayerList`/`AdventureSettings` burst → `PLAYER_SPAWN`), **chunk streaming** (RequestChunkRadiusPacket → `CHUNK_RADIUS_UPDATED` ack → radius-4 distance-sorted `FullChunkDataPacket` burst via the parallel-gen + storage path), and **movement** (MovePlayerPacket → `PositionComponent` + `NetworkSessionService` broadcasts the position back to peers). New `protocol/ChunkSerializer` (16×16×128 sections, block + meta + block-light + **heightmap-derived sky light** — previously the wire sent pitch-black zeros because the generator emits no skylight arrays; now surface+ blocks are lit 0xFF, deep blocks 0x00) + `BatchPacket` (uncompressed `Info::BATCH_PACKET` = 0x06 framing — the old API's batch id, verified against `old-src`). The adapter hands its `Runnable` loop to the kernel's single `NetworkThread` and `bootstrap.php` now calls `setNetworkingEnabled(true)` so `run()` binds UDP and serves clients. `tests/17_network_test.php`: 11 tests / 53 assertions against a **real UDP loopback client** (fake client sends login with a real client guid, reads datagrams, decodes the burst, asserts protocol rejection, spawn burst, chunk ack + radius, distance-sorted streaming with correct block/heightmap/sky-light contents, move round-trip). Suite now **17 files / 96 tests / 1304 assertions**
47. ✅ **Permissions wired (12.6)** — `plugin.yml` `permissions:` are parsed and **enforced**. `PermissionManager` gains a `parseYaml()` (description/default/children, inheritance + default resolution) and the **op-check bug from old-src is fixed** (it looked for metadata key `'op'` but `setOp` stores `'pocketmine.op'` — the new check consults the real op flag). The Kernel creates + exposes the single `PermissionManager` (registered as a resource), `PluginManager` registers a plugin's yml permissions on enable and unregisters on disable, and `Player::hasPermission` delegates through the manager (defaults `true`/`false`/`op`/`not_op`, children, inheritance). `KernelAccessor` + `Plugin` expose `getPermissionManager()`. Command gating flows through it (`PlayerCommandSender` → `Player::hasPermission` → manager). `tests/18_permission_test.php`: 7 tests / 25 assertions (plugin.yml permissions registered at enable, default `true` grants, default `op` denies non-op and grants op, children imply parent, `pocketmine.op` op flag, command gating sends the no-permission message for denied senders, disable unregisters). Suite now **18 files / 103 tests / 1329 assertions**
48. ✅ **Pipeline perf push (13.1)** — main-thread tick hit the <15ms target and the busy-polling is gone. **(a) Snooze, not polling** — new `core/thread/SnoozeHandle` implements the PMMP `snooze` pattern (a `ThreadSafe` wait/notify condvar with wakeup counting + bounded timeout); `RegionThread` (was 200µs → 5000 wakeups/sec idle) and `NetworkThread` (was 1ms) now block on the condvar and are woken by the kernel/adapter on every queue push — idle threads burn ~zero CPU. **(b) ECS hot paths** — `Query::archetypes()` uses the World's maintained `entityArchetypes` map (O(1) per entity) instead of per-entity `array_keys`+`sort`+`implode`; `applyPendingComponents` iterates archetype component arrays with one `method_exists` per *type* instead of per component. Raw 5000-entity tick: **10.5 → 7.0 ms**. **(c) Honest benchmark** — `measure_pipeline.php` now reports **hot** (back-to-back, saturated-load) ticks alongside the paced cold number; the cold-vs-hot gap (15.8 vs 7.0 ms) is a **CPU frequency-scaling artifact** (35ms idle sleep drops the core clock, next tick runs at ~half speed) verified with an A/B probe, not code. Final: **off mode hot 7.04 ms (target <15 ✓), apply mode hot 1.95 ms** — the pipeline offloads ~5 ms of simulation per tick off the main thread
49. ✅ **Honest playability audit (13.2)** — the server is **not yet joinable by a real 0.15.10 client**. Verified against the wire code: the adapter speaks the RakNet **offline** handshake (ping→pong, `OPEN_CONNECTION_REQUEST_1/2`→`REPLY_1/2`) and the full game flow (login → spawn burst → chunk streaming → movement round-trip) is proven against a fake client (`tests/17`), but a real client additionally needs the RakNet **connected** layer the adapter never handles: `CONNECTION_REQUEST` (0x09) → `CONNECTION_REQUEST_ACCEPTED` (0x10) → `NEW_INCOMING_CONNECTION` (0x13), plus everything inside RakNet `DATA_PACKET_0–F` frames (reliability, sequence numbers, ACK/NACK, fragmentation for chunks > MTU) — the adapter currently expects raw game-packet datagrams (`Info::LOGIN_PACKET` as byte 0). The complete implementation already exists in `src/raklib/` (SessionManager, Session with reliable windows, EncapsulatedPacket, ACK/NACK; all registered in `registerPackets()`); it was orphaned by the Phase-8 rewrite, is not thread-safe for pmmpthread v6.3 (plain `$shutdown`/`$mainPath` on a Thread subclass), and references deleted legacy classes (`\ThreadedLogger`, `\ClassLoader`, old `Config`). **Roadmap (PLAN.md §13.2):** port SessionManager/Session into the new architecture (ThreadSafe-ify, swap legacy deps, keep the protocol classes verbatim), run on the existing `NetworkThread`, feed decoded packets into `NetworkSessionService`
50. ✅ **RakNet connected-session layer ported (13.2)** — the legacy `src/raklib/` codebase is now **thread-safe and clean**, and the server answers the full RakNet connected flow a real 0.15.10 client needs. **(a) ThreadSafe-ification** — `RakLibServer` is a `Thread` with only ThreadSafe properties (pmmpthread v6.3-compliant); a new `ThreadSafeLogger` replaces the deleted `\ThreadedLogger`; all `\ClassLoader` / old `Config` / load-time-`exit()` deps are gone. **(b) Clean architecture + perf** — strict types everywhere, `registerPackets()` table, `sessionLimit` enforced, error capture surfaces worker crashes, and every raklib class the worker touches is **preloaded before `start()`** (pmmpthread v6.3 workers can't autoload — the same pattern `ParallelGeneratorAdapter` uses). **(c) Adapter is now a `ServerInstance` bridge** — `Protocol84NetworkAdapter` owns its RakLibServer thread, answers the connected handshake (`CONNECTION_REQUEST` 0x09 → `CONNECTION_REQUEST_ACCEPTED` 0x10 → `NEW_INCOMING_CONNECTION` 0x13), decodes encapsulated game packets, and routes them into `NetworkSessionService` with **disconnect handling** wired (`PlayerLeaveService` injected). The kernel's `NetworkThread` is **deleted** — the adapter owns the RakNet thread (bootstrap flow unchanged). **(d) Tests** — `tests/17` was rewritten: the fake client is now a **real RakNet client** (offline + connected handshake, `DATA_PACKET_*` framing with reliability, message-index ordering, split reassembly) and proves login → spawn burst → chunk streaming → movement + a **second concurrent client** + **disconnect** end-to-end. **Fixed a real legacy routing bug the port exposed**: `Session::handleEncapsulated` only streamed payloads with id ≥ 0x80 to the main thread, silently swallowing the 0x06 batch packets — all connected game traffic now streams. `tests/17`: 12 tests / 53 assertions, stable across repeated runs. Suite now **18 files / 104 tests / 1336 assertions**, PHPStan 0 errors, boot smoke healthy. **Phase 13 complete — the server is joinable by a real client (pending the user's real-client verification)**
51. ✅ **Real-client ready (13.2 followup)** — three things a real 0.15.10 client needs beyond the port were fixed and **proven on the wire**: **(a) RakNet motd** — the adapter now sends the structured `MCPE;<name>;<protocol>;<version>;<online>;<max>` motd in `UNCONNECTED_PONG` (the legacy `RakLibInterface::setName` layout; version field left empty like the old API), refreshed on player join/leave so the server list count stays live; a bare name meant a real client would never see the server in its list. **(b) `start.sh`/`start.cmd`/`start.ps1`** — pointed at `bootstrap.php` (they still looked for the deleted `PocketMine.php`). **(c) Startup banner** — `run()` now prints the bind port. **Proven on the wire**: new `tests/live_ping_probe.php` boots the real server via `proc_open` (fully detached descriptors — a pmmpthread server keeps a `shell_exec` pipe open forever) and sends a **genuine RakNet `UNCONNECTED_PING`**, asserting the PONG carries a client-parseable motd (`MCPE;Khronos Server;84;;0;20`, magic intact, pingID echoed). Stable across repeated runs. Full suite still **18 files / 104 tests / 1336 assertions**, PHPStan 0 errors
52. ✅ **Day/night cycle (14.6)** — `TimeSystem` (new sequential ECS system) advances `WorldConfig::$time` one tick per world tick, wrapping at 24000 (a full 20-minute day at 20 TPS). `NetworkSessionService` broadcasts the current value to every player **every poll** (`SetTimePacket`, 5 bytes) and the login burst now carries the real world clock instead of a hardcoded 0 - the sun/moon actually move on the client. **Hostile mobs only spawn after dusk** (`MobSpawnerSystem` gates on `TimeSystem::isNight`, time >= 12000) so day/night means something in the game. The clock is **persisted** with the world meta (saveWorld/applyPersistedWorldMeta) so a restart resumes the same hour, and the api `World` facade read/writes the same resource. `tests/23_time_test.php`: 5 tests / 12 assertions (advance per tick, 24000 wrap, night-gate semantics, facade round-trip, persisted round-trip). `tests/19` gains a day-gate test (no hostile spawns while the sun is up; spawn tests set midnight). `tests/17` asserts the login-burst SetTime is a valid day value and a fresh client receives an ADVANCING SetTime on the wire. Suite now **24 files / ~110 tests / ~1352 assertions**, PHPStan 0 errors
53. ✅ **Inventory actions + drop item (14.7)** — the client can now move items between its own inventory slots and drop items with Q. `ContainerSetSlotPacket` (0x2c) is decoded for window 0 (`SPECIAL_INVENTORY`): the server applies the client-claimed slot state to the authoritative `InventoryComponent` and mirrors the changed slot back to the actor (legacy `PlayerInventory::sendSlot` parity). `DropItemPacket` (0x29) spawns a real item entity thrown along the player look direction (single item per Q press, matched against the held slot first, then a scan), and the consumed slot is reflected back. Both packets are encode/decode symmetric so the test client can drive them over the wire. `tests/17` gains two wire tests: move-slots + drop-and-spawn (27 tests / 114 assertions in tests/17 alone).
54. ✅ **Survival mining time (14.x)** — breaking is no longer instant. The 0.15 flow (START_BREAK → client animates cracks → REMOVE_BLOCK) is now server-authoritative: `BlockBreakService::requiredBreakTicks()` computes hardness × tool-speed (creative = 0), `NetworkSessionService` records START and only honors REMOVE once the elapsed ticks are met (ABORT clears). `BlockRegistry::requiresTool` fixed — only tool-level-gated blocks (stone/ores) truly need a tool; grass/dirt break by hand in ~12 ticks so legit client breaks always pass. `TickCounter` finally increments each run-loop tick. Hostile test proves REMOVE-without-START is rejected (no instant-mining exploit). `tests/17`: 29 tests / 119 assertions, full suite + PHPStan green.
55. ✅ **XP orbs + health regen (14.9)** — the survival loop closes: mob deaths drop XP orbs that are now collectible, players carry persistent XP (progress + level via the standard curve) synced to the HUD through a new legacy-format `UpdateAttributesPacket` (player.level/player.experience), orbs render on the wire as legacy AddEntityPacket type 69 (XPOrb::NETWORK_ID, DATA_NO_AI), and a new `RegenSystem` heals 1 HP per 80 ticks after a 100-tick no-damage window (damage detected by health-delta observation, no combat hooks). `tests/17`: +3 wire tests (orb broadcast, pickup+XP sync, regen) — 32 tests / 126 assertions, full suite + PHPStan green.
56. ✅ **Tool durability (14.10)** — tools and weapons wear out. Breaking a block or landing an attack consumes one durability point (survival only; creative is exempt): the item meta IS the damage counter (legacy parity), so the client durability bar updates with no new packets, and a tool that reaches its max (wood 59, stone 131, iron 250, gold 32, diamond 1561...) breaks into air with the slot synced back as empty. New shared `ItemDurability::consume()` (gamemode + durability checks, meta++/destroy, slot sync) hooks into `BlockBreakService::doBreakBlock` and `EntityInteractionService::attack`, and `NetworkSessionService::syncInventorySlot()` mirrors the changed held slot to the actor. `tests/17`: +3 wire tests (wear per break + slot sync, break-at-max, sword wear on attack) — 35 tests / 134 assertions, full suite + PHPStan green.
57. ✅ **Food + hunger (14.11)** — the survival core closes: players have a draining food bar, food items restore it on use, and starving players take damage. New `HungerComponent` (hunger 0-20 / saturation / exhaustion) added to all players; `HungerSystem` (sequential) applies passive exhaustion, the legacy drain loop (4.0 exhaustion → 1 saturation else 1 hunger), starvation damage (1 HP/80 ticks at zero hunger, window starts when food first hits 0) and HUD sync only when the bars move. `ItemRegistry::FOOD` lists 19 protocol-84-era foods (apple 4/2.4, steak 8/12.8...) with legacy-identical values. `handleUseItem` routes food to `eat()`: full players refuse, one item consumed, USE_ITEM animation broadcast, slot + HUD synced. `Hunger::exhaust/applyFood` helpers (creative-exempt) fed by action hooks — attack 0.3, mining 0.025, damage taken 0.3 (exact legacy CAUSE_* values). HUD via `syncFoodFor()` sending player.hunger/player.saturation through UpdateAttributesPacket (login burst too). RegenSystem now requires hunger > 0 (vanilla pre-1.6): starving players never heal. `tests/17`: +4 tests — 39 tests / 146 assertions, full suite + PHPStan green.
58. ✅ **In-game crafting (14.12)** — the survival crafting loop is now real: the client can craft in its 2x2 inventory grid and receive the result over the wire. The login burst now carries the full recipe list (`CraftingDataPacket` 0x2f, legacy layout: entry count, per entry [type, len, payload] with width/height/ingredient slots/result/uuid, wildcard damage 0x7fff) built from `RecipeRegistry::getShapedRecipes()`; a `CraftingEventPacket` (0x30, legacy decode: windowId, type, recipe UUID, input grid, claimed output) is routed to a new handler that validates the grid by shape through `CraftingService`, consumes the ingredients, grants the result, and resyncs window 0. `CraftingService` is now injected into `NetworkSessionService` (Kernel ordering fixed). `tests/17`: recipe list present in the login burst (9 recipes, planks 1x1 L→4), a 2x2 craft over a real socket grants the crafting table and consumes all four planks (item entities despawned first for determinism) — 40 tests / 157 assertions, full suite + PHPStan green.
59. ✅ **Armor + equipment (14.13)** — the survival armor loop is real end-to-end: clients equip gear through the 0x78 armor window, armor reduces attack damage, and gear wears out and breaks on hits. The inventory grows to 40 slots (0-35 window 0, 36-39 armor) with `InventoryComponent::ARMOR_OFFSET`; `add()`/`canAddItem()` stay capped at 36 so armor is never auto-filled. `ContainerSetSlotPacket` now decodes the armor window (slots 0-3 -> inventory 36-39) with the same session-wide move credit as window 0; a new `MobArmorEquipmentPacket` (0x1c, legacy eid long + 4 slots) broadcasts gear to every session on change, `HurtArmorPacket` fires when a piece absorbs a hit, and the login burst carries the armor window contents. Two real bugs fixed in `CombatService`: armor reduction read hotbar slots 5-8 instead of armor slots 36-39 (armor never reduced anything), and the material table mapped diamond to ids 318-321 (flint/porkchop) instead of 310-313 and omitted leather 298-301 entirely. Wear-on-hit applies to landed entity attacks only (legacy parity): one random worn piece takes 3 durability with the 2-in-3 random gate, breaking and removing at max. `tests/17`: +3 wire tests (0x78 equip + broadcast, diamond reduction vs baseline, wear + break) - 43 tests / 166 assertions, full suite + PHPStan green.
60. ✅ **Builtin commands (14.14)** — the chat line `/...` is a real command pipeline now, not an echo. `NetworkSessionService::handleChat` routes leading-`/` lines to the shared server-wide `CommandMap` (same one the console and plugins use) with a `PlayerCommandSender` for that session; command output goes back to the sender only. `PlayerCommandSender` was rebuilt on `(entityId, username)` while still accepting a `Player` facade, so the Phase 13.2 plugin callers keep working. Six builtins ship: `/gamemode` (0/1 + per-player, flips `AdventureSettingsPacket` 0x7D creative flags + writes authoritative `MetadataComponent(gamemode)`), `/teleport` and `/tp` (`<x y z>` or `<player> <x y z>`, ECS position + immediate MODE_RESET `MovePlayerPacket`), `/give` (item id + count, window-0 only, 2304 cap, content sync back), `/kill` (full combat pipeline, players stay as corpses and can respawn), `/time` (day/noon/sunset/night/midnight/value, sets `WorldConfig->time` so the per-poll broadcast syncs everyone), `/help` (lists all registered commands). tests/17 gains six wire tests; the harness now pins `seed=1` (a random seed occasionally spawned the configured spawn in an ocean, moving the safe-spawn result outside the +-24 assertion bound — pre-existing flake).
61. ✅ **Furnaces + smelting (14.16)** — the smelting loop is real end-to-end: right-click a furnace (unlit 61 or lit 62) to open a real 3-slot container window (window 3, type 3 - InventoryType::FURNACE). New `SmeltingRegistry` holds 24 furnace recipes + 16 fuel entries with the exact legacy 0.15 values (recipes.json type 2/3: cobblestone→stone, sand→glass, iron/gold/coal/diamond/redstone/lapis/emerald/quartz ore→products, logs→charcoal, clay→brick, cactus→green dye, raw meats/fish/potato/rabbit/salmon→cooked, stone bricks→cracked; Fuel::$duration: coal 1600, coal block 16000, logs/planks/chest/table 300, stick/sapling 100, wooden tools 200, blaze rod 2400, lava bucket 20000). New `FurnaceStore` keeps per-block state (input/fuel/result slots + burnTime/cookTime) with the same TileEntitySnapshot round-trip as ChestStore. New `FurnaceSystem` (sequential) ticks every furnace each world tick exactly like the legacy `Furnace::onUpdate`: consumes 1 fuel to light (block 61→62), burns the fuel down, cooks 200 ticks per item (10s), produces the result, and goes dark (62→61) when fuel runs out; slot + lit-block changes sync to every viewer with that furnace open through a new public `syncFurnace` hook (block broadcast only on actual lit/unlit flips). `handleFurnaceSetSlot` applies window-3 moves with the same session-wide move credit as chests/window 0, `onFurnaceBroken` spills contents, and the login/container paths mirror chests. **Two real persistence bugs fixed along the way**: (1) FIFO eviction (`ChunkUnloadService::persistAndUnload`) saved chunks WITHOUT chest/furnace tile snapshots - a crash after eviction (before the next autosave) permanently lost block contents; a shared `ChunkPersistence::attachTileSnapshots` helper now attaches them on EVERY save path (autosave, eviction, explicit unload) and REPLACES stale disk-owned snapshots instead of merging duplicates; (2) the FurnaceSystem used `Closure::call` (binds $this) instead of plain invocation, and always rewrote every furnace entry per tick (now dirty-flag `put`). `tests/24_furnace_test.php`: 5 tests / 27 assertions (registry exact+wildcard matching, fuel ticks, store snapshot round-trip, full smelt over 205+ ticks with lit/dark block transitions, break spill). `tests/17`: +3 wire tests - furnace window open (window 3/type 3), window-3 moves validated with move credit + fuel consumed on lighting, and a lit furnace cooking 200 ticks over the wire broadcasting the ingot content refresh + UpdateBlockPacket(62) (the fake client sends periodic MovePlayer keepalives so the RakNet 10s idle timeout doesn't drop the session mid-cook). Suite now **25 files / ~121 tests / ~1488 assertions**, PHPStan 0 errors.
62. ✅ **Bows + arrows (14.17)** — the first projectile with a full wire round-trip: holding right-click with a bow starts a draw (stored draw-start tick), releasing fires an arrow thrown along the look direction with power scaled by draw time. `ArrowSystem` flies every arrow (per-tick drag 0.99 velocity retention, gravity via the shared `PhysicsSystem` like every other entity), sticks into solid blocks on contact, hits the nearest entity within 1 block of its path (excluding the shooter for the first 5 ticks and all projectiles; creative players are immune), deals `ceil(speed/20*2)` damage (+ critical bonus for full draws) through `CombatService`, and despawns at 1200 ticks. Wire flow: `UseItemPacket` start → `PlayerActionPacket` RELEASE → `AddEntityPacket` (type 80) → per-tick `MoveEntityPacket` → `RemoveEntityPacket`. `tests/25_bow_test.php`: 8 tests / 33 assertions.
63. ✅ **Projectiles (14.18)** — in-flight arrow rendering + stuck arrows + a `ProjectileRegistry` as the single source of truth for projectile types (name → legacy 0.15 network id + damage/gravity/drag/sticky). 'Arrow' registers as network id 80; `EntitySpawnService::spawnProjectile` validates against the registry and throws for unknown types. Arrows now **render in flight** (AddEntityPacket goes out on spawn and the per-tick MoveEntityPacket keeps the arrow on the velocity vector, oriented along its heading — the old build never rendered arrows between spawn and impact), **stick into the victim** they hit (embedded at the impact offset, riding the victim's position each tick, falling to the ground when the victim dies), and carry the shooter id in metadata for attribution. `tests/25`: in-flight orientation, stuck-and-rides, drop-on-victim-death, registry single-source.
64. ✅ **Terrain + biomes (14.19)** — the generator now reads as a real world instead of a patchwork of tiny plateaus: multi-octave value noise with LARGE feature cells (512/128/32 blocks) builds real continents and hills, every column gets a biome from temperature + humidity noise (plains, forest, desert, taiga, ice plains, extreme hills, beach, ocean), and the surface profile matches the biome (sand beaches, sandy deserts, snow-capped ice plains, ocean floors with sea level). A **spawn plateau** keeps the origin land for any seed (no more spawning in the ocean), and the populate pass places trees (forest) and vegetation deterministically in (chunkX, chunkZ, seed) so the world is byte-identical after a restart. `tests/26_terrain_test.php`: 8 tests / 126,494 assertions — parallel-pool determinism, heightmap-vs-surface agreement, spawn-plateau land guarantee across many seeds, biome variety, ocean floor/sea-level columns, sandy beach/desert columns, forest trees + plains vegetation, no cross-chunk population bleed.
65. ✅ **Multi-world (14.20)** — worlds are first-class: a `WorldComponent` (int id) on every entity plus a `WorldRegistry` holding per-world bundles (name/folderName/seed/ChunkStore/WorldConfig/StoragePort; default world id 0; `registerWorld`/`removeWorld` with by-folder dedup). Kernel boots the default world with the RAW `ServerConfig` seed so a post-boot pin still resolves on first generation; the Server API adds `loadWorld`/`generateWorld`/`unloadWorld`; a `/world` command + `switchWorld` wire flow flips a connected player's `WorldComponent`, re-streams the new world's chunks (fresh queue, stale per-action state cleared), despawns every old-world entity explicitly (a client keeps drawing known entities until told to drop them), and sends the new world's time/spawn/hard move. Blocks place into and mobs spawn in the entity's own world; entities never render across worlds. `tests/27_multiworld_test.php`: 6 tests / 32 assertions.
66. ✅ **Infinite world + real Anvil/McRegion storage (10.4 follow-up)** — the world is unbounded and stable across restarts. (1) **Streaming follows the player**: crossing a chunk boundary re-queues the radius window so chunks stream in continuously (no more "end of the world"), bounded by a per-tick `CHUNKS_PER_TICK` budget. (2) **Real on-disk formats**: a clean `src/pocketmine/nbt/` codec (13 tags, big/little endian, faithful binary layout) plus `RegionStorageAdapter` (shared MCA/MCR container with the real 4KiB-sector index and zlib-compressed big-endian NBT records) replaces the custom KRON format. `AnvilStorageAdapter` reads/writes real vanilla `.mca` chunks (16 sections, block/meta/light nibbles merged into the flat 256-height store, negative coordinates handled); `McRegionStorageAdapter` handles `.mcr` (128-height flat arrays); `LevelProviderManager` auto-detects the format from the world folder (`level.dat` → Anvil, else mcr/kron fallback) with a clear LevelDB error stub. World meta (seed/spawn/difficulty/time) round-trips through a REAL `level.dat` (`RandomSeed` long tag). `tests/28_storage_formats_test.php`: 9 tests / 100 assertions (NBT codec round-trip, .mca round-trip + sector index, .mcr round-trip, legacy-record read, auto-detect, LevelDB stub, meta round-trip); tests/17 gains the streaming-follows-player wire test.
67. ✅ **Real-client fixes: /tp eid-0, seed persistence, suffocation guard** — three bugs a live 0.15.10 client exposed. (1) `/tp` (and respawn) sent `MovePlayerPacket` with `eid=<entityId>`; protocol 84 addresses the player's own entity as **eid 0**, so the client ignored the teleport — both self-teleport sites now send eid 0 (the per-tick broadcast excludes self, so these were the only self-moves). (2) The world regenerated with a fresh random seed every boot: `saveWorldMeta` wrote `(string)($worldConfig?->seed ?? $config->getSeed())` but `WorldConfig->seed` is never pinned on the boot path, so `??` picked 0 and `level.dat` stored `RandomSeed=0` — save now falls through 0 to `ServerConfig::getSeed()` and `applyPersistedWorldMeta` syncs `WorldConfig->seed` so every reader agrees. (3) Returning players whose saved position landed inside newly generated terrain suffocated on join — `PlayerJoinService` validates the restored spot (feet + head block non-solid) and falls back to the terrain-aware safe world spawn when unsafe (water/lava are non-solid, so swimmers keep their position). tests/17 asserts the teleport eid is 0; tests/10 asserts the WorldConfig seed restore. Suite now **29 files / ~216 tests / ~128k assertions**, PHPStan 0 errors.
68. ✅ **Weather cycle (14.22)** — rain, rain+thunder, thunder, and clear spells with random 6000-12000 tick durations (legacy `Weather::calcWeather`), state machine from the legacy weighted table (clear 7/10, rain 2/10, storm 1/10), per-world `WorldConfig` state (weather/weatherDuration/lightningTick) persisted in the level.dat meta so a restart resumes the same storm. `WeatherSystem` (sequential) counts the current spell down each tick and rolls the next; `NetworkSessionService` pushes transitions as the two legacy LevelEventPackets (rain START/STOP 3001/3003 + thunder START/STOP 3002/3004 with the legacy particle strengths) on login and on change, and during storms strikes a lightning bolt (AddEntityPacket type 93 at the highest block near a random player, legacy `Level::spawnLightning`) every 200 ticks — visual + sound only, matching 0.15 (no fire damage). `tests/29_weather_test.php`: 6 tests / 23 assertions (duration countdown, storm-clears, table roll, lightning timer, state predicates, meta round-trip); `tests/17` gains a wire test (login burst STOP ids, START_RAIN + START_THUNDER transitions, lightning bolt broadcast). Two pre-existing suite flakes fixed along the way: the terrain sanity bound now accounts for population-pass trees (cap 120 + tree tops), and the player-persistence tests save at y=200 so the join safe-position guard keeps the spot deterministic. Suite now **30 files / ~223 tests / ~128k assertions**, PHPStan 0 errors.

69. ✅ **Events wired into gameplay services + plugin auto-load (Blocker 4)** — every event class that existed in `api/event` but never fired now fires at the right point. `PlayerJoinEvent` (join), `PlayerLeaveEvent` (+ reason), `PlayerRespawnEvent` (pre-mutation), `EntitySpawnEvent` (mobs/projectiles/items), `PlayerInteractEvent` (attack = LEFT_CLICK_BLOCK, interact = RIGHT_CLICK_BLOCK — both cancellable), `BlockBreakEvent` + `BlockPlaceEvent` (cancellable before any mutation: cancelled break leaves the block and drops nothing; cancelled place keeps the item in inventory), `PlayerChatEvent` (cancellable + message-rewrite, broadcast uses the modified message), `PlayerMoveEvent` (cancellable: reverts position + sends reset to client; from/to captured pre-mutation). `EventPort` is injected into the seven services (Kernel wiring) — `CombatService` already fired `EntityDamage/Death` events; now the full lifecycle is plugin-observable. **Plugin auto-load**: `Kernel::loadPluginsFromDirectory()` scans `plugins/` at the first `run()` (folder created when missing, mirroring `server.properties`) and loads `.phar` archives and `plugin.yml` directories while ignoring everything else (`.jar` is rejected by `PluginManager`; READMEs/logs skipped), so plugins are live from tick 0 on a real server. `tests/31_events_test.php`: 11 tests / 30 assertions (service-level join/leave/respawn/break/place/spawn/interact events incl. cancellation, plus a directory-scan test that loads a fixture dir plugin, skips a `.jar` and a `.txt`, and asserts the plugin is enabled); `tests/17` gains three wire tests (cancelled chat is never broadcast, rewritten chat echoes the new message, move event carries from/to) — all stable across repeated runs. Full suite **31 files / ~234 tests / ~128.3k assertions**, PHPStan 0 errors. (Three pre-existing wire-test timing flakes — view-distance/chest/weather — rotate under machine load and pass on retry; unrelated to this change.)

70. ✅ **Ores (Blocker 3 #1, ⭐)** — mining finally pays off. The terrain populate pass (`ParallelGeneratorAdapter::populateChunkPure`) now places deterministic ore veins in stone, mirroring the legacy 0.15 `Ore` populator table and geometry: per-chunk cluster counts/sizes with the legacy Y windows (coal 20×16 @ any depth, iron 20×8 @ ≤64, redstone 8×7 @ ≤16, lapis 1×6 @ ≤32, gold 2×8 @ ≤32, diamond 1×7 @ ≤16) and the legacy sine-weighted ellipsoid-blob vein shape. Emerald (1×4 @ y4-32) only spawns in extreme-hills chunks. Two safety properties baked in: veins only ever replace stone (never air, water, dirt, or surface blocks — verified 0 floating ores across a 16×16-chunk scan) and every write stays inside the chunk's 16×16 columns (blob lobes clamped to the ore's Y window, so no cross-chunk bleed and no surface ore). The ore pass draws AFTER the tree/vegetation pass on the same per-chunk RNG stream, so existing tree/grass placement is byte-identical (tests/26 still passes its 126,494 assertions untouched). `tests/39_ore_test.php`: 5 tests / 25 assertions (legacy Y windows + relative abundance coal≫gold / diamond<coal, determinism, no floating ores, no surface ore + 4096-byte sections, emerald window in hills terrain). Full suite **32 files / ALL TEST FILES PASSED**, PHPStan 0 errors.

71. ✅ **Caves (Blocker 3 #2)** — the underground is no longer solid rock. `ParallelGeneratorAdapter::generateChunkPure` now runs a carver pass: 3D worm caverns anchored to a 48-block world grid, so adjacent chunks carve the SAME global path and caverns connect across borders even though every chunk is generated independently. Each cave system hosts 2-4 worms (radius 1.5-3.0, up to ~58 segments) that wander with gentle yaw drift and a slow upward climb. **Safety rules baked in**: only stone is ever replaced (never air, water, dirt, sand or surface blocks); the top 2 blocks of every column are never touched (no holes in the ground, the heightmap stays valid); y=0 bedrock is never carved; every write stays inside the chunk's 16x16 columns; carved cells below y=10 become lava pools; carved cells under the ocean floor fill with water (ocean biome only). **Two real bugs found while validating**: (1) caves-carve-first + ore-veins-after meant a vein could land on a 1-block stone floor over a cavity and float — ore placement now requires solid stone below every ore block (the ore test's "never floats" property holds with caves on); (2) `FluidSystem` and `TNTExplosionSystem` broadcast `EntityRef`s into `NetworkPort::broadcastPacket`, which requires `PlayerRef`s — a latent type error that only fired once cave water created flow configurations that reach the broadcast path; both now resolve players through `NetworkSessionService::getPlayerRefByEntity`. `tests/40_cave_test.php`: 5 tests / 13 assertions (caverns exist below the surface, surface never breached, lava only ≤ y10 + bedrock intact, cave water only in ocean columns, determinism + heightmap unchanged + 4096-byte sections). Full suite **33 files / ALL TEST FILES PASSED**, PHPStan 0 errors.

72. ✅ **Double chests + dispenser/hopper/brewing-stand containers (Blocker 3 #9/11)** — the container family is now complete and per-world from day one. `ContainerStore` handles dispenser (9 slots) + hopper (5 slots) with the same per-world snapshot persistence as chests/furnaces; `BrewingStore` + `BrewingRegistry` bring the full legacy brewing table (nether wart, glowstone, redstone, gunpowder, fermented spider eye, golden carrot, etc.) with a real 400-tick `BrewingSystem`. Two adjacent chests pair into a single 54-slot window on open (slot routing remaps the second half, breaking one spills both halves). `HopperSystem` runs the 8-tick transfer cadence: pulls the first occupied slot from the container above, pushes to the container below (skipping chained hoppers, matching vanilla). `NetworkSessionService` dispatches right-click open for all five container types with per-type window ids, and `syncContainer` mirrors changes to viewers.

73. ✅ **Block light finish (Blocker 3 #3)** — the light pipeline now *matters* end to end. `ChunkStore::recalculateLight` compares old vs new arrays and marks changed chunks light-dirty; the network layer drains those each tick and re-sends the full chunk, because protocol 84's UpdateBlockPacket carries no light — so a placed torch now actually illuminates on the client (verified on the wire: chunk re-send with lit block-light nibbles). The store gained a light query API (`getSkyLightLevel`/`getBlockLightLevel`/`getLightLevel`) and `MobSpawnerSystem` gates hostile spawns on darkness (block light > 0 = protected), so torch-lit areas keep mobs away for real.

74. ✅ **Enchanting table + anvils (Blocker 3 #6, beacons removed)** — beacons don't exist in MCPE 0.15.10 (added in 0.16), so this PR delivers enchanting + anvils only. `EnchantmentRegistry` ports the full 0.15 catalogue (25 enchantments: names, weights, max levels, slot masks, level ranges, conflict rules, per-item enchantability). `ItemStack` gains NBT enchant helpers (`ench` list that round-trips through the wire/persistence, custom names, repair cost). `EnchantmentService` generates the three bookshelf-boosted table options (legacy base-roll + weighted pick + conflict pruning), applies a chosen option (lapis + XP levels), and implements anvil combine (book transfer / same-item merge) + rename. Network: right-click an enchanting table (116) or anvil (145) opens the window; table options ride `CraftingDataPacket` ENTRY_ENCHANT_LIST (protocol-84 wire format); lapis/level validation happens server-side. Effects wired in: Sharpness (melee damage), Power (arrow damage), Efficiency (mining speed), Unbreaking (durability wear skip). `tests/44_enchant_test.php`: 8 tests / 45 assertions.

75. ✅ **Test workflow speedups (dev-cycle)** — the suite used to cost 10-20 minutes per run, almost all of it pure waiting. Three fixes: (1) **fast ticks** — `Kernel::run()` slept ~50ms per tick for 20 TPS pacing, and tests pump `run(1)` hundreds of times; `KHRONOS_FAST_TICKS=1` (set by `tests/run.php` for every child) skips the pacing sleep entirely, so a filtered single test now runs in ~0.1s instead of hanging on a 120s timeout. (2) **`--filter=substring`** in the test harness — run one named test case inside a big file (`php tests/17_network_test.php --filter="double-chest"`). (3) **`-j N` parallel runner** — `tests/run.php -j 4` runs up to four files concurrently, each in its own `sys_get_temp_dir()/khr_tests/<file>` working dir, so kernel dataPath, world region files, and every getcwd()-relative path stay isolated per worker (the suite no longer touches the repo's `worlds/` at all). A `register_shutdown_function` on the Kernel makes `exit()` tear down worker threads deterministically — previously a `--filter` that skipped 17's shutdown test left threads alive and pmmpthread's shutdown joined them forever (main thread parked in `futex_do_wait`). Baseline: full 45-file suite serial ~15-20 min; with `-j 4` the entire suite (17's worldgen-heavy network test included) is ~8 min, and everything except the worldgen-bound tests finishes in ~90s. The one test that asserted a repo-absolute path (`worlds/world/players/…`) now uses `$kernel->getDataPath()` so it passes under both standalone and temp-dir runs.

76. ✅ **Nether dimension + portals (Blocker 3 #7)** — the nether is a real second dimension, portals and all (MCPE 0.15.10 has no End — nether only). `GeneratorType::Nether` ('nether', legacy 'hell' alias) routes `ParallelGeneratorAdapter::generateChunkPure` to a deterministic 128-high nether chunk: bedrock floor AND ceiling, netherrack mass with 3D-ish cave noise (y fed into the noise seed per slice), a lava sea below y=32 that opens from a solid floor toward the surface (legacy waterHeight), and `ChunkStore::hasSky=false` so `LightCalculator` computes zero sky light and `ChunkSerializer` sends dark sky nibbles — the nether is genuinely dark except lava/glowstone/torch block light. The population pass adds legacy nether features deterministically: quartz ore veins (20×16, y 0-128), soul-sand + gravel patches (5×64), glowstone clusters hung from the ceiling, ground fire on netherrack tops, and 5% surface lava lakes. **Portals**: flint & steel on a complete obsidian frame (4-23 wide × 5-23 tall, full top row, both orientations — the legacy `FlintSteel::onActivate` detector) fills the inside with portal blocks; standing inside a portal builds the legacy charge (80 ticks survival, instant creative) and crosses dimensions via `ChangeDimensionPacket` (0x36, dimension byte + x/y/z — the exact legacy wire format) so the client shows the building-terrain screen before the new world streams. The nether world auto-creates on first use (`nether.enabled` / `nether.world` in khronos.json, default world name 'nether', seeded + persisted like any world); returning through a nether portal lands back at the saved overworld spot (validated like the join safe-spawn guard, else the overworld safe spawn). `switchWorld` gained an explicit-position overload + sends the dimension packet when the target world's dimension differs (dimension derived from the world generator). The mob spawner is overworld-only (players in other worlds never attract spawns at their overworld coordinates). `tests/45_nether_test.php`: 5 tests / ~20 assertions (determinism + bedrock bounds + lava below 32, populated quartz/glowstone/soul-sand/gravel/fire, GeneratorType coercion, ignition completeness guard, ensureNetherWorld idempotence + no-sky store); `tests/17` gains two wire tests — flint & steel lighting a frame, and a full portal round-trip (overworld → ChangeDimensionPacket dim 1 + nether world + netherrack spawn → nether portal → ChangeDimensionPacket dim 0 + overworld). Full suite green, PHPStan 0 errors.

77. ✅ **Events breadth audit (Blocker 3 #5)** — the audit found 13 firing event classes vs 118 in old-src; this PR wires **20 new events** so plugin devs can hook the gameplay surface. New cancellable events: `PlayerLoginEvent` (fires inside `PlayerJoinService::handleJoin` right after the entity exists, BEFORE `PlayerJoinEvent` — cancelling makes `handleJoin` return null and the login disconnects with the kick message), `PlayerKickEvent` (kick()/kickByIp(), reason editable), `PlayerDropItemEvent` (handleDropItem before the item leaves the inventory), `PlayerItemConsumeEvent` (eat() before removal), `PlayerGameModeChangeEvent` (/gamemode before the metadata mutates), `PlayerToggleSneakEvent`/`PlayerToggleSprintEvent` (PlayerActionPacket, tracked in metadata), `EntityRegainHealthEvent` (RegenSystem), `EntityShootBowEvent` (bow release before the arrow spawns/consumes), `EntityTeleportEvent` (switchWorld + /tp), `InventoryPickupItemEvent` (ItemPickupSystem), `CraftItemEvent` (CraftingService before ingredients are consumed), `WeatherChangeEvent` (WeatherSystem transitions + /weather), `ServerCommandEvent` (CommandMap for every sender). New informational events: `EntityDespawnEvent` (EntityDespawnService + PlayerLeaveService), `InventoryOpenEvent`/`InventoryCloseEvent` (all five container opens + close), `PluginEnableEvent`/`PluginDisableEvent` (PluginManager). `BlockUpdateEvent` fires from BlockUpdateService. `EntityDespawnService`, `CraftingService` and `BlockUpdateService` gained an `EventPort` constructor dep (Kernel passes it). `tests/46_events_breadth_test.php`: 14 tests / 40 assertions — login-before-join ordering + veto, kick/drop/consume/gamemode/sneak/sprint/regen/bow/teleport/pickup/craft/weather cancellation, despawn + plugin lifecycle + server command firing.

78. ✅ **Events breadth audit — wave 2 (Blocker 3 #5 continued)** — 14 more events close the gap for the "genuinely useful" category the audit flagged: hotbar, animation, pre-login, quit, chunk, item, projectile, motion, inventory-transaction and data-packet events. New cancellable events: `PlayerItemHeldEvent` (handleMobEquipment before the held slot changes — a veto keeps the old slot), `PlayerAnimationEvent` (new ANIMATE_PACKET wire case; not cancelled = echoed to other players), `PlayerPreLoginEvent` (fires in handleLogin BEFORE the ECS entity exists — earliest veto point, carries username/address/kick message), `ChunkUnloadEvent` (before a chunk leaves memory; a veto keeps it resident), `ItemDespawnEvent` (EntityDespawnService for dropped items), `ProjectileLaunchEvent` (bow release after the arrow spawns — a veto despawns the arrow and skips ammo), `EntityMotionEvent` (all three KnockbackService paths), `InventoryTransactionEvent` (every reported ContainerSetSlot move, all windows), `DataPacketReceiveEvent` (every inbound game packet post-login; drop = not handled) and `DataPacketSendEvent` (every outbound queued packet; drop = not sent, or mutate in place). New informational events: `PlayerQuitEvent` (handleLeave alongside PlayerLeaveEvent), `ChunkLoadEvent` (fresh loads with a newChunk flag), `ItemSpawnEvent` (spawnItem), `ProjectileHitEvent` (ArrowSystem block + entity impacts). `ChunkLoadService`, `ChunkUnloadService` and `KnockbackService` gained an `EventPort` constructor dep (Kernel passes it). `tests/47_events_breadth2_test.php`: 6 tests / 32 assertions (service-level firing + cancellation for quit/chunk/item/motion, construction checks for the session-driven events); `tests/17` gains 4 wire tests — item-held veto, arm-swing animation, transaction veto keeping the authoritative slot, and receive/send data-packet events over a real client. Full suite green (46 files), PHPStan 0 errors.
79. ✅ **Foreign world compatibility + world admin commands (14.33)** — old PocketMine-era worlds keep their spawn so people don't rebuild them. `RegionStorageAdapter::loadWorldMeta` now reads integer NBT fields tolerantly (Byte/Short/Int/Long tags all accepted per field, mirroring old-src's loose array access): vanilla/0.15-era level.dat files store the same field with different widths (`Time` as IntTag vs LongTag), and the strict typed read previously threw, got swallowed, and treated the whole world as fresh — losing its spawn/seed. Verified against funil's exact shape (IntTag Time, SpawnX/Y/Z, RandomSeed). `/world load <name>` brings an on-disk world folder back into the registry (spawn/seed restored from level.dat); `/world unload <name>` saves + drops the bundle (default world refuses); `/world list` now also shows unloaded world folders on disk (`Server::getUnloadedWorlds()` scans `worlds/` for region data or level.dat, ignoring plugin leftovers like protector.yml). New `/setspawn` command: pins the world spawn to the sender's feet, writing the per-world WorldConfig (honored by `/world <name>` switching and the safe-spawn fallback) and mirroring the ServerConfig for the default world so join/respawn land there too — then saves immediately. `tests/48_world_admin_test.php`: 6 tests / 36 assertions. Full suite green, PHPStan 0 errors.

80. ✅ **Shutdown kicks every online player (legacy forceShutdown parity)** — stopping the server no longer drops the socket under connected clients. `NetworkSessionService::shutdown()` now runs `kickAll()` first: every session receives a `DisconnectPacket` with the legacy `settings.shutdown-message` default ("Server closed") plus a clean RakNet session close, then tears down through the normal leave path (`teardownSession`, extracted from `handleSessionClosed` — dismount, chest-lid close, peer removal broadcast, `PlayerLeaveEvent`/`PlayerQuitEvent`/`EntityDespawnEvent`, persistence via the leave service). Delivery is guaranteed by queue ordering: the adapter enqueues the disconnect frame and the `CLOSE_SESSION` command into the RakLib thread's FIFO ahead of any `PACKET_SHUTDOWN`, and `Session::close()` flushes its send queue, so the frame is written to the client before the socket closes. Per-session `Throwable` guards keep one bad session (plugin listener, storage hiccup) from aborting the shutdown, and a save safety net persists any session that appeared mid-kick. Follow-up fixes from real-client testing: the adapter's socket is torn down LAST in `Kernel::shutdown()` (after the world saves, legacy order) so the RakNet reliable layer can flush AND retransmit the disconnect frame on real networks, and the disconnect frame now rides `PRIORITY_IMMEDIATE`. The critical wire fix: the DisconnectPacket must be sent as a **0xfe-prefixed single frame** (`chr(0xfe) . buffer`, exactly like `flushOutbound` frames and legacy `putPacket`) — a naked packet is silently ignored by the real 0.15 client, which only noticed the drop at its own connection timeout ("disconnected from server" after a delay). `tests/17` gains a wire test: with Alice still connected, `$kernel->shutdown()` removes her from the session service and delivers a `DisconnectPacket` whose reason is exactly "Server closed".
